import { View, Text, StyleSheet } from "react-native";
import React from "react";

export default function RenderRightActions() {
  return <View></View>;
}
const styles = StyleSheet.create({
  container: { width: 70, backgroundColor: "purple" },
});
